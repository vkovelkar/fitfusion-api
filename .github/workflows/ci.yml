name: FitFusion CI

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

env:
  DOCKER_IMAGE: vkovelkar/fitfusion-api

jobs:

  test:
    name: Test FitFusion Application
    runs-on: ubuntu-latest

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Upgrade pip
        run: python -m pip install --upgrade pip

      - name: Install Python dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest requests

      - name: Install Azure Functions Core Tools
        run: |
          npm install -g azure-functions-core-tools@4 --unsafe-perm true

      - name: Start Azure Functions
        run: |
          func start --python --port 7071 > function.log 2>&1 &
          sleep 15

      - name: Check Azure Functions startup
        run: |
          cat function.log

      - name: Wait for Azure Functions
        run: |
          for i in {1..30}; do
            if curl -f http://localhost:7071/api/health; then
              echo "Azure Functions is ready"
              exit 0
            fi

            echo "Waiting for Azure Functions..."
            sleep 2
          done

          echo "Azure Functions failed to start"
          exit 1

      - name: Run tests
        run: |
          pytest tests/ -v


  docker:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    needs: test

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ env.DOCKER_IMAGE }}:${{ github.sha }}
            ${{ env.DOCKER_IMAGE }}:latest


  kubernetes:
    name: Validate Kubernetes Manifests
    runs-on: ubuntu-latest
    needs: test

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install kubectl
        uses: azure/setup-kubectl@v4

      - name: Validate Kubernetes manifests
        run: |
          kubectl apply --dry-run=client --validate=false -f k8s/